# Clone or navigate to your repo
cd claurafeng.github.io

# Delete the file
git rm _pages/charity-correlation.md

# Commit the deletion
git commit -m "Delete charity-correlation.md"

# Push to remote
git push origin master

